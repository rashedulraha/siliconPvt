// This file re-exports from the new CMSContext location.
// Kept for backwards compatibility — all logic now lives in context/CMSContext.tsx
export { CMSProvider, useCMS, initialState } from "@/context/CMSContext";
